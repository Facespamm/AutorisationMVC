# Этап сборки
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build

WORKDIR /src

COPY ["AutorisationMVC.csproj", "./"]

RUN dotnet restore "AutorisationMVC.csproj"

COPY . .

RUN dotnet publish "AutorisationMVC.csproj" \
    -c Release \
    -o /app/publish \
    /p:UseAppHost=false


# Финальный контейнер
FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS final

WORKDIR /app

COPY --from=build /app/publish .

ENV ASPNETCORE_URLS=http://+:8080
ENV ASPNETCORE_ENVIRONMENT=Production
ENV DOTNET_hostBuilder__reloadConfigOnChange=false

EXPOSE 8080

ENTRYPOINT ["dotnet", "AutorisationMVC.dll"]